{
  "framework": "vite",
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "api/_shared/**",
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/a/:slug",
      "destination": "/index.html"
    }
  ]
}
